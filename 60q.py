#u means Unicode string. Unicode allows Python to store characters from many languages (English, Hindi, Chinese, etc.)
# In Python 3, all strings are Unicode by default, so the u is optional
print(u"hello world")
print(u"नमस्ते")